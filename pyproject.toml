[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "git-python-starter"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
todo = "apps.cli_todo:main"

[tool.setuptools]
packages = ["apps", "lib"]

